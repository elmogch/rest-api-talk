version: '3'
services:
  api:
    image: rest-api-talk
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app

  web-cli:
    image: rest-api-talk
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge